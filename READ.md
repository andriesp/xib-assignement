# XIB Assignment

## Getting started.

Build the project to download the dependencies
```shell script
mvn clean install
```

Run the application:
```shell script
mvn spring-boot:run
```

Application can be accessed on http://localhost:8080